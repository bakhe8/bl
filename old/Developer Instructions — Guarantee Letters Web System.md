📄 Developer Instructions — Guarantee Letters Web System
نسخة المبرمج — الإصدار 1.0

هذه الوثيقة موجّهة للمبرمج المسؤول عن تطوير النظام الجديد الخاص بتجهيز خطابات تمديد الضمانات البنكية، اعتمادًا على واجهة الويب الحالية (index.html) ومستودع الأصول (assets.zip).

🎯 1. الهدف العام للنظام

النظام المطلوب هو أداة تعمل داخل المتصفح (Browser-only)، بدون Backend، وظيفتها:

رفع ملف Excel يحتوي معلومات الضمانات البنكية.

تحليل البيانات واستخراج الحقول الرئيسية.

تحديد البنك والمورد لكل صف باستخدام قاموس رسمي.

في حال وجود غموض → عرض نافذة قرارات للمستخدم لحلّ الخيارات.

توليد خطاب HTML جاهز للطباعة → Print → Save as PDF.

تخزين نتائج التعلم (Variants) محليًا في LocalStorage.

📦 2. الملفات المعتمدة (Assets)

النظام يعتمد على الملفات الموجودة داخل assets.zip:

داخل assets.zip يتوفر:

index.html (الواجهة الرئيسية – يجب البناء فوقها وليس استبدالها)

خط AL-Mohanad (Bold + ExtraBold) — للاستخدام في الخطاب الرسمي

مجلد assets/ — يحتوي الصور والستايلات الأساسية

ملاحظة:

يجب عدم تغيير أسماء هذه الملفات، فقط استخدام ما فيها.

🧩 3. معمارية العمل (Core Architecture)

سيعتمد النظام على ٦ وحدات رئيسية:

✔ 1) excelParser.js

قراءة ملف Excel

استخراج الأعمدة:

رقم الضمان

رقم العقد

البنك

المورد

المبلغ

تاريخ الانتهاء

تطبيق Normalization آمن

مخرجاته مصفوفة JSON جاهزة للمرحلة التالية

✔ 2) matchingEngine.js

مسؤول عن:

مطابقة اسم البنك بالقاموس الرسمي

مطابقة اسم المورد بالقاموس الرسمي

استخدام “variants” للتعلم الآلي البسيط

إرجاع:

auto_matched

أو needs_confirmation

✔ 3) dictionaryStore.js

تخزين القواميس في LocalStorage:

البنوك الرسمية

الموردين الرسميين

الصور المتعددة (Variants)

سياسة صارمة:

لا يسمح بإضافة بنك جديد إلا إذا تم اختياره يدويًا

المورد الجديد يضاف فقط إذا المستخدم اختاره بنفسه

لا يوجد Undo → لذلك يجب إضافة تأكيد للمستخدم قبل إضافة variant جديد

✔ 4) modal.js — (Modal v2)

نقطة الارتكاز في واجهة النظام.

الوضعيات الأربع:

الوضع	الاستخدام
decision	حلّ غموض البنك/المورد
alert	رسائل النظام والتنبيهات
print	عرض الخطاب الجاهز للطباعة
loading	أثناء معالجة Excel
✔ 5) letterBuilder.js

إنشاء البيانات النهائية للخطاب:

تنسيق التاريخ بالصيغة العربية الرسمية

تنسيق المبلغ بفواصل

التحكم في أسماء البنك والمورد

✔ 6) letterTemplate.html

قالب HTML داخل النافذة (Modal Print Mode)
لا يجب أن يكون في ملف خارجي — سيتم حقنه مباشرة داخل الـ modal.

🧱 4. مبدأ المعالجة (Workflow)

التسلسل الإجباري:

1) يرفع المستخدم ملف Excel

→ modal.loading()

2) parser → استخراج القيم

→ قائمة JSON

3) matchingEngine

→ لكل صف:

auto_matched

أو needs_confirmation

4) إذا كان هناك صف يحتاج تدخل

→ modal.decision()
ويستمر المستخدم حتى إنهاء جميع الصفوف

5) بناء بيانات الخطاب

→ letterBuilder()

6) عرض الخطاب

→ modal.print()

📜 5. قواعد العمل (Rules & Constraints)
🔴 1) لا دعم للترميز غير UTF-8

أي ملف Excel يظهر داخله علامات "؟؟؟" → يرفض مباشرة.

🔴 2) لا يتم قبول أي بنك جديد من نص إنجليزي

الخيارات:

الاسم الرسمي الموجود في القاموس

أو اختيار يدوي من المستخدم

أو حفظه بدون إضافة للقاموس

🔴 3) نظام التعلم (variants) بسيط فقط

variant جديد يضاف فقط في حال اعتماد من المستخدم

لا يتم الاعتماد على fuzzy إلا لتقديم اقتراح

لا يوجد learning تلقائي بدون تدخل

🔴 4) لا يوجد Undo

لذلك يجب:

نافذة تأكيد قبل إضافة أي variant

لا يتم دمج variant مع أخرى تلقائيًا

🎨 6. واجهة المستخدم (UI Specs)
✔ استخدام نفس نافذة الجافا داخل index.html

لكن تطويرها إلى Modal v2:

تقسيم النافذة إلى:

modal-header

modal-body

modal-footer

إضافة CSS Class لكل وضع:

modal-decision

modal-alert

modal-print

modal-loading

دعم RTL كامل
استخدام خط AL-Mohanad للخطابات
📏 7. متطلبات الجودة (Quality Standards)

زمن فتح خطاب ≤ 0.2 ثانية

زمن تحليل Excel ≤ 1 ثانية لـ 50 صف

لا تحديث للصفحة نهائياً

لا استخدام jQuery أو أي مكتبة UI

لا استخدام IndexedDB

فقط:

JavaScript Vanilla

LocalStorage

FileReader API

SheetJS (للقراءة فقط)

🧪 8. حالات الاختبار (Test Cases)
✔ ملفات Excel بسيطة
✔ ملفات Excel بإنجليزي + عربي
✔ بنك غير معروف → decision modal
✔ مورد غير معروف → decision modal
✔ ملف فيه عمود ناقص → alert modal
✔ خطاب يتم عرضه ثم طباعته عبر نافذة المتصفح
🔐 9. الأمان (Security Rules)

رفض أي ملف XLSM

رفض الملفات التي تحتوي more than 1000 row

منع حقن HTML داخل الخطاب (sanitize قبل الحقن)

لا يتم حفظ أي ملف خارج المتصفح

لا يتم استخدام fetch أو AJAX

النظام يعمل 100% Offline

📂 10. هيكل المشروع المقترح (Project Structure)
project/
│ index.html
│ main.js
│ style.css
│
├── modules/
│   ├── excelParser.js
│   ├── matchingEngine.js
│   ├── dictionaryStore.js
│   ├── modal.js
│   ├── letterBuilder.js
│   └── utils.js
│
├── data/
│   └── dictionaries.json   (اختياري - تصدير/استيراد)
│
└── assets/
    ├── fonts/
    ├── imgs/
    └── styles/

🟦 11. تعليمات للمبرمج — لا يجوز مخالفتها

لا تستخدم أي مكتبة UI.

لا تغير index.html إلا لتطوير نافذة modal.

لا تنشئ صفحات HTML إضافية.

لا تستخدم jQuery إطلاقاً.

لا تحفظ أي بيانات خارج LocalStorage.

لا تستخدم Backend — مشروع Front-end فقط.

modal.js هو المركز الأساسي للواجهة.

matchingEngine.js يجب أن يكون deterministic (نتيجة ثابتة).

الخطابات تبنى من قالب HTML يتم حقنه في modal.print.

النظام يجب أن يعمل 100% دون إنترنت.