# .env.example.ar — مثال متغيرات بيئة (عربي)
#
# القيم التالية أمثلة ذات شكل واقعي لاستخدامها مع Vite (الجانب العميل).
# ملاحظة مهمة: لا تضع مفاتيح أو أسرار حقيقية في هذا الملف إذا كان مُرتبَطاً بمستودع Git.
# انسخ الملف إلى `.env.local` أو `.env` وأدخل القيم الحقيقية المحلية هناك.
# في Vite يجب أن تبدأ المتغيرات التي يحتاجها الكود من جهة العميل بـ `VITE_`.

# بيانات التطبيق
VITE_APP_TITLE=تطبيق_خطابات_الضمان          # اسم التطبيق الظاهر في العنوان
VITE_DEFAULT_LOCALE=ar-SA                    # اللغة/المنطقة الافتراضية
VITE_RELEASE_VERSION=1.0.1                   # نسخة التطبيق (اختياري)

# API الخلفية
VITE_API_URL=https://api.example-company.com  # نقطة نهاية الـ API (استبدل بالقيمة الحقيقية)
VITE_API_TIMEOUT=10000                        # مهلة طلبات الـ API بالميلي ثانية

# مفاتيح وخدمات خارجية (أمثلة شكلية)
VITE_MAPS_API_KEY=pk.YOUR_PUBLIC_MAP_KEY      # مفتاح عام لخدمات الخرائط (مثال)
VITE_GA_MEASUREMENT_ID=G-XXXXXXX             # Google Analytics measurement id (غير حساس عادة)

# تتبع الأخطاء / المراقبة
VITE_SENTRY_DSN=                              # ضع هنا DSN لـ Sentry إن وُجد (لا ترفع للـ Git)
VITE_SENTRY_ENV=development                    # environment name for monitoring

# إعدادات البريد (مُستخدم عادة من الخادم؛ هذه مفاتيح توضيحية فقط)
VITE_MAILER_ENABLED=true                       # flag لتمكين واجهة إرسال البريد (frontend flag)
VITE_MAILER_FROM=no-reply@example-company.com  # البريد الافتراضي للمرسل
VITE_MAILER_SMTP_HOST=smtp.example-mail.com    # مضيف SMTP (مثال)
VITE_MAILER_SMTP_PORT=587                      # منفذ SMTP
VITE_MAILER_SMTP_USER=mailer@example-company.com
VITE_MAILER_SMTP_PASS=                         # ضع كلمة المرور الحقيقية في `.env.local`

# تكوينات بيانات/ملفات
VITE_SUPPLIER_DATA_URL=https://data.example-company.com/suppliers  # مثال لعنوان بيانات الموردين

# Feature flags
VITE_ENABLE_ANALYTICS=true
VITE_FEATURE_PREVIEW=false

# ملاحظات أمنية
# - لا تضع أسراراً حقيقية (مثل كلمات المرور، مفاتيح خاصة) في أي ملف يُدخَل للمستودع.
# - احتفظ بالأسرار في `.env.local` أو في خدمة إدارة أسرار (Vault, GitHub Secrets, etc.).

# نهاية الملف
