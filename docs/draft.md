📄 setup.md – تعليمات تشغيل بيئة التطوير
1. نظرة عامة

الهدف من هذه الوثيقة هو تقديم تعليمات تشغيل واضحة وآمنة، تمنع:

تغيير المنافذ تلقائيًا

ظهور صفحة بيضاء

توقف الطرفية في VS Code

مشاكل الشبكة

مشاكل المسارات في Vite

جميع التعليمات هنا تعمل على Windows / VS Code.

2) متطلبات التشغيل (Prerequisites)

Node.js v18 أو أحدث

npm

VS Code

لا تستخدم Live Server أو Python server نهائيًا
(لمنع تعارضات المسارات)

3) تشغيل المشروع على Vite
3.1 تثبيت التبعيات

داخل مجلد المشروع:

npm install

3.2 تشغيل Vite دائمًا على منفذ ثابت (5173)

يجب تثبيت المنفذ في ملف:

vite.config.js

export default {
  server: {
    host: true,
    port: 5173,
    strictPort: true
  }
};

معنى الإعدادات:
الخاصية	التفسير
host: true	يسمح بالوصول إلى الصفحة من الشبكة المحلية
port: 5173	المنفذ الرسمي لتشغيل المشروع
strictPort: true	يمنع تغيير المنفذ في حال كان مشغول
النتيجة:

✔ إذا كان المنفذ مشغولًا → يظهر خطأ واضح
❌ لا يحاول Vite تغيير المنفذ إلى 5101 أو 5102 أو غيره

4) تشغيل السرفر في نافذة مستقلة (غير معطّلة لواجهة VS Code)

أفضل طريقة لتشغيل السرفر دون تعليق:

Start-Process -FilePath "npm" -ArgumentList @("run","dev") -WindowStyle Minimized


🔹 هذا يشغّل Vite في نافذة PowerShell مستقلة
🔹 تبقى نافذة VS Code قابلة للاستخدام
🔹 لا يحدث تعليق على الطرفية

5) إيقاف السرفر بدون إغلاق VS Code

استخدم الأمر:

Get-Process node | Stop-Process -Force


✔ يوقف سرفر Vite فورًا
✔ لا يغلق VS Code
✔ يحل مشكلة “تراكم عمليات Node”

يفضل تشغيل هذا الأمر قبل تشغيل السرفر من جديد.

6) فتح المشروع في المتصفح

رابط المشروع:

http://localhost:5173/

7) حل مشكلة "صفحة بيضاء"

إذا ظهرت صفحة فارغة:

افتح DevTools في المتصفح:

اضغط F12

تبويب Console

غالبًا سترى أحد الأخطاء التالية:

7.1 "Failed to load module"

السبب:

مسار خاطئ في index.html

أو ملف ناقص

الحل:
تأكد من وجود هذا في index.html:

<script type="module" src="/src/main.jsx"></script>

7.2 "404 /src/main.jsx"

السبب:
الملف غير موجود أو اسمه مختلف.

الحل:

تأكد من وجود /src/main.jsx

أو /src/main.tsx بحسب المشروع

7.3 صفحة بيضاء بدون أخطاء

غالبًا يعني:

React component لم يتم تصديره بشكل صحيح

أو Syntax error في JSX غير ظاهر

الحل:

شغّل السرفر مرة أخرى

أرسل رسالة الخطأ (إن وجدت) وسيتم إصلاحها

8) حل مشكلة تغيّر المنافذ (5101 → 5102 → 5103…)

هذه المشكلة تحدث عندما:

لا يتم استخدام strictPort: true

أو يتم تشغيل المشروع على Live Server

أو يتم تشغيل Vite من عدة طرفيات دون إيقافها

الحل النهائي:

فقط:

ضع strictPort: true

أوقف كل عمليات Node:

Get-Process node | Stop-Process -Force


شغّل من جديد:

Start-Process -FilePath "npm" -ArgumentList @("run","dev")

9) منع التعارض مع VS Code

لتحقيق تشغيل نظيف:

✔ لا تستخدم Live Server
✔ لا تستخدم python -m http.server
✔ لا تستخدم أي سيرفر غير Vite
✔ شغّل Vite فقط باستخدام الأوامر المذكورة
✔ شغّل السرفر في نافذة مستقلة دائمًا

10) وضع التشغيل المثالي للمطور

الخطوات الرسمية:

أوقف عمليات Node:

Get-Process node | Stop-Process -Force


شغّل Vite في نافذة مستقلة:

Start-Process -FilePath "npm" -ArgumentList @("run","dev")


افتح المتصفح:

http://localhost:5173/


ابدأ التطوير في VS Code بحرية

عند الانتهاء:

Get-Process node | Stop-Process -Force

11) Troubleshooting — حل أشهر مشاكل التشغيل
✔ Vite يعمل لكن الصفحة فارغة

افتح الـConsole وارسِل لي الخطأ.

✔ يظهر لي ترميز غريب داخل Vite (مثل â)

هذا طبيعي، بسبب Encoding في PowerShell
لا يؤثر إطلاقًا على التشغيل.

✔ يظهر "Port 5173 is in use"

الحل:

Get-Process node | Stop-Process -Force


ثم أعد تشغيل السرفر.

✔ أغيّر الكود لكن الصفحة لا تتحدث

افتح المتصفح على http://localhost:5173/

قد تكون فتحت عنوانًا خاطئًا مثل 5100/5101.

12) ملاحظات للمبرمج

يرجى الالتزام بالبساطة في الهيكلة

تجنب إنشاء ملفات زائدة

عدم تشغيل أي سيرفر إضافي

عدم استخدام Webpack أو أدوات أخرى

لا تضيف طبقات غير ضرورية

لا تعتمد Modules متعددة متشابكة

حافظ على الملفات قصيرة وواضحة

كل شيء يجب أن يعمل من صفحة واحدة