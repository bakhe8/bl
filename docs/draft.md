1️⃣ docs/letter_template.md — الفجوة: القالب غير محدد بالكامل
✔ المشكلة:

لا يوجد النص الرسمي الكامل

لا يوجد تحديد الخط

لا يوجد تحديد حجم الخط

لا يوجد margins للطباعة

لا توجد طبقة منع اختلافات PDF بين المتصفحات (Chrome/Edge)

✔ الحل المقترح:

سأضيف للوثيقة:

🔷 1) النسخة النهائية من نص الخطاب (كاملة + رسمية)

– كما تم تعريفها في توثيق المشروع
– مع placeholders واضحة

🔷 2) مواصفات الخط الرسمية:

Arabic UI Font → "Noto Naskh Arabic", "Segoe UI", serif

الحجم:

المتن: 16px

العنوان: 18px

line-height: 1.8

RTL enforced via:

html, body { direction: rtl; unicode-bidi: bidi-override; }

🔷 3) منع اختلافات طباعة PDF عبر CSS:
@media print {
  @page { size: A4; margin: 20mm; }
  body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
}

🔷 4) تثبيت الخط داخل الصفحة إن لزم (WebFonts)

حتى لا تختلف الطباعة بين الأجهزة.

→ سيتم توثيق كل هذا داخل letter_template.md.

2️⃣ docs/dictionaries.md — الفجوة: لا يوجد تنسيق رسمي للأسماء + لا توجد سياسة تعارض
✔ المشاكل:

لا يوجد Rule موحد لكتابة الأسماء الرسمية (Format)

لا يوجد حل إذا حدث تصادم بين officialId محتملين

لا يوجد حدود لحجم LocalStorage

لا يوجد Versioning للقواميس

✔ الحل:
🔷 1) صيغة رسمية إلزامية للأسماء:

البنوك:

“اسم البنك كما تنشره المؤسسة الرسمية (SAMA)”
مثال:

“مصرف الراجحي”

“البنك السعودي للاستثمار”

الموردين:

“الاسم التجاري الرسمي العربي كما يظهر في العقد أو فاتورة المورد”.

الضوابط:

بدون تطويل

بدون تشكيل

بدون اختصارات

🔷 2) سياسة فضّ التعارض:

إذا تطابق اسم خام مع أكثر من officialId:

يعرض النظام للمستخدم "دعوة اختيار"

لا يحفظ variant إلا بعد الموافقة

🔷 3) ضبط LocalStorage:

الحد نضعه 4MB (الحد العالمي)

إذا اقترب الحجم من 90% → يظهر تحذير

عند التصدير → دمج الـ variants المتقاربة لتقليل الحجم

🔷 4) إضافة Schema Version للقواميس:
{
  "version": "1.0.0",
  "generated_at": "2025-01-12",
  "banks_official": [...],
  "banks_variants": {...}
}


→ سيتم إضافتها داخل docs/dictionaries.md.

3️⃣ docs/excel_processing.md — الفجوة: لم نحدد قواعد كشف الأعمدة والencoding
✔ المشاكل:

ما حددنا قواعد اكتشاف الأعمدة

ما حددنا التعامل مع encoding (UTF-8 vs CP1256)

ما حددنا التعامل مع أرقام مخزنة كنص والعكس

لا يوجد سيناريو فشل واضح عند mapping مشبوه

✔ الحل:
🔷 1) قواعد اكتشاف الأعمدة:

exact match

lowercase match

normalized Arabic match

List of all synonyms per column

مثال:

bank: ["bank", "bank name", "اسم البنك", "البنك", "BANK"]

🔷 2) ترميز النصوص (Encoding):

قبل قراءة الخلايا:

نحاول parsing UTF-8

إذا ظهرت أحرف مربعة ☐ → نعتبر الملف CP1256 ونحوّله

🔷 3) أرقام مخزنة كنص:

إذا كانت الخلية تحتوي أرقام فقط → تحويل إلى Number

إذا كانت تحتوي فواصل عربية/إنجليزية → تنظيف ثم رقم

إذا كانت تحتوي نص + رقم → يحتفظ بالرقم كـ string ويحذّر المستخدم

🔷 4) سياسة الفشل:

إذا تعذر اكتشاف أحد الأعمدة الإلزامية → رفض الملف

إذا كان mapping غير منطقي (مثلاً عمود واحد يطابق حقلين) → تحذير أحمر

→ سيتم إضافتها في excel_processing.md

4️⃣ docs/matching_algorithms.md — الفجوة: لم نحدد العتبات الرقمية ولا سياسة fuzzy
✔ المشاكل:

Fuzzy بدون أرقام = خطر

عدم وجود سياسة “عدم التغيير تحت حد الثقة”

عدم الدمج لمنع تضخم variants

✔ الحل:
🔷 1) العتبة الرقمية (Threshold):

التطابق المباشر: 100%

fuzzy acceptable ≥ 85%

بين 70%–85% → عرض على المستخدم

أقل من 70% → مرفوض تلقائيًا

🔷 2) الخوارزمية المستخدمة:

Levenshtein distance

Jaro Winkler (احتياطي)

🔷 3) قاعدة أمان:

إذا كانت الثقة أقل من 85% → لا يسمح للنظام باختيار الاسم بدون موافقة المستخدم.

🔷 4) دمج variants المتشابهة عند التصدير:

إذا كان الفرق بين variant1 وvariant2 أقل من 10% → يتم دمهما بشكل تلقائي أثناء التصدير.

→ سيتم إضافتها.

5️⃣ docs/system_requirements.md — الفجوة: متطلبات الأمن والأداء غير دقيقة
✔ المشاكل:

لم يتم تقييد أنواع الملفات

لا يوجد حد لحجم الملف

لا يوجد معيار جهاز اختبار

لا توجد حدود للذاكرة

✔ الحل:
🔷 1) أنواع الملفات المسموحة:

.xlsx فقط

يمنع .xls, .xlsm, .csv

رفض أي ملف ماكرو Macro-enabled

🔷 2) الحد الأقصى لحجم الملف:

1MB (يكفي 1000 صف)

أكثر من ذلك → تحذير

🔷 3) الأداء:

يجب معالجة 100 صف خلال أقل من 1 ثانية

معيار الاختبار:

جهاز: i5, 8GB RAM

متصفح: Chrome latest

🔷 4) الذاكرة:

عدم تحميل كامل Excel في DOM

استخدام SheetJS بصيغة ArrayBuffer فقط

→ سيتم إضافتها.

6️⃣ docs/system_design.md — الفجوة: لا يوجد versioning ولا حماية DOM
✔ الحل:
🔷 1) إضافة schema versioning للملفات:

مثال:

schema_version: 1.2.0

🔷 2) آلية ترقية:

إذا وجد النظام نسخة قاموس قديم → يقوم بترقيته تلقائيًا

إذا وجد mapping قديم → يعيده للصيغة الجديدة

🔷 3) حماية DOM من XSS:

أي بيانات تأتي من Excel يجب أن تمر عبر دالة sanitize:

function sanitize(text){
  return text.replace(/[<>]/g, "");
}

7️⃣ docs/ui_spec.md — الفجوة: لا يوجد WCAG ولا رسائل خطأ موحدة
✔ الحل:
🔷 1) إمكانية الوصول (Accessibility):

contrast ratio 4.5:1

زر "Tab" يتنقل بشكل طبيعي

كل صفحة تحتوي lang="ar"

🔷 2) رسائل خطأ موحدة:

كل رسالة يجب أن تكون بصيغة:

“تعذّر قراءة عمود (اسم المورد). الرجاء تحديد العمود الصحيح.”

وتوثيق جميع الرسائل في جدول.

8️⃣ docs/testing.md — الفجوة: لا يوجد اختبارات طباعة PDF ولا tests للقواميس
✔ الحل:
🔷 تضاف سيناريوهات:

اختبار الطباعة (Chrome/Edge)

اختبار RTL داخل PDF

اختبار استيراد القواميس

اختبار تجاوز LocalStorage

اختبار mapping الخاطئ

اختبار Fuzzy الثقة المنخفضة

9️⃣ docs/data_safety.md — الفجوة: LocalStorage limits + corrupt JSON
✔ الحل:
🔷 1) توثيق الحد المسموح:

100KB للقواميس max
إذا تجاوز → طلب “تفريغ القواميس القديمة”

🔷 2) Checksum عند الاستيراد:
{
  "checksum": "sha256:abcxyz",
  "version": "1.0.0",
  "data": {...}
}


النظام يتحقق قبل قبول الملف.

📌 الخلاصة:

كل فجوة تم تحويلها الآن إلى قرار هندسي جاهز للإضافة داخل التوثيق.