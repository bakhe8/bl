فيما يلي وثيقة تعليمات تفصيلية للمبرمج — جاهزة لتسليمها كما هي، وتشمل:

طريقة التشغيل باستخدام Vite داخل VS Code بدون أي تعارض

قواعد البساطة التي يجب أن يلتزم بها المبرمج أثناء التطوير

توجيهات تقنية دقيقة لضمان سهولة الاستخدام ومنع تضخم النظام

أفضل ممارسة للحفاظ على أداء عالي وبناء نظيف

الوثيقة مكتوبة باحتراف لتكون مرجعًا رسميًا داخل المشروع.

🟩 وثيقة تعليمات رسمية للمبرمج
Guidelines for Implementing the Guarantee Letters Web App (Vite + VS Code)
1) الهدف من الوثيقة

هذه الوثيقة توضح للمبرمج:

كيفية إعداد وتشغيل المشروع باستخدام Vite دون أي تعارض

التزامات ضرورية للحفاظ على البساطة وعدم تضخم البرنامج

قواعد العمل داخل VS Code لضمان تشغيل نظيف وسريع

قواعد تطوير متسقة مع رؤية صاحب المشروع: البساطة والوضوح والسرعة

2) قواعد البساطة – Core Simplicity Rules

يجب الالتزام الحرفي بالمبادئ التالية أثناء التطوير:

✔ 2.1 النظام يجب أن يبقى بسيطًا جدًا

لا إضافات غير ضرورية
لا صفحات زائدة
لا تفرعات معقدة
لا أكواد “ذكية” تزيد التعقيد بدون فائدة مباشرة للمستخدم.

✔ 2.2 كل شيء يجب أن يتم داخل صفحة واحدة Only One Page

رفع ملف

القرارات

المعاينة

الطباعة

الإعدادات البسيطة

✔ 2.3 أي بانل يمكن طيه/فتحه (Collapse / Expand)

هدفه:
تجنب التشتت البصري + السماح للمستخدم بالتركيز.

✔ 2.4 ممنوع التوسع في القواعد أو الذكاء الصناعي

لا تعلم ذاتي غير مضبوط
لا خوارزميات متوحشة
لا fuzzy غير مقيّد
كل شيء بسيط، مباشر، قابل للتفسير.

✔ 2.5 لا نريد نظام متعدد الصفحات أو Router

We want SPA without routing.

3) تعليمات التشغيل — العمل داخل VS Code مع Vite
3.1 لماذا اخترنا Vite؟

سريع

يدعم React و Tailwind

لا يحتاج سيرفر backend

يعمل كـ "سيرفر محلي صغير" فقط لسبب فني: تشغيل ES Modules

3.2 لا يوجد أي تعارض مع VS Code

VS Code لا يشغل سيرفرات بنفسه
وبالتالي Vite يعمل بشكل منفصل تمامًا دون مشاكل.

4) تعليمات إعداد بيئة العمل خطوة بخطوة
✔ 4.1 أنشئ المشروع (إن لم يكن جاهزًا)
npm create vite@latest guarantee-app --template react
cd guarantee-app
npm install

✔ 4.2 اجعل Vite يستخدم منفذ ثابت ومضمون

أنشئ ملف:

vite.config.js
export default {
  server: {
    port: 5100,      // المنفذ الرئيسي
    strictPort: false // إذا مشغول ينتقل أوتوماتيكياً للمنفذ التالي
  }
}


⚡ فائدة:

عدم وجود تعارض مع أي مشروع آخر

حتى لو كان 5100 مشغول → Vite سيستخدم 5101 → 5102 تلقائيًا

✔ 4.3 تشغيل المشروع

داخل VS Code:

npm run dev


سيفتح المتصفح:

http://localhost:5100


أو منفذ أعلى لو كان مشغول.

5) ممنوع استخدام Live Server

Live Server لا يدعم ES Modules
→ سيعطّل تحميل الأكواد
→ سيجعل الأزرار لا تعمل

المشروع يجب أن يعمل فقط عبر Vite.

6) بنية المشروع المطلوبة
project/
   index.html
   vite.config.js
   package.json
   src/
      App.jsx
      components/
      styles/

7) قواعد أساسية في الواجهة (UI / UX Rules)
7.1 جميع البانلات قابلة للطي

الأخطاء

حلّ القرارات

تفاصيل السجل

المعاينة

7.2 عند الضغط على صف داخل “حل القرارات”

يجب تنفيذ الآتي:

يظهر Panel صغير أسفل الصف مباشرة

يعرض خيارات تحل التعارض للبنك والمورد

لا نفتح نافذة منبثقة

لا ننتقل إلى صفحة أخرى

7.3 عند اختيار بنك/مورد

يتم حفظ الاختيار تلقائيًا

لا يوجد زر حفظ

التحديث يحدث فورًا أينما يعرض السجل

7.4 المعاينة (Letter Preview)

تتحدث تلقائيًا عند تغيير أي بيانات

لا تحتاج زر تحديث

تظهر في Panel منفصل على اليمين

8) التعليمات الخاصة بالقرارات (Decision Logic)
✔ 8.1 التعامل مع البنك

إذا الاسم الخام غير موجود في قائمة البنوك الرسمية → عرض اختيار للبنك

لا يتم اعتماد أي بنك جديد بشكل تلقائي

إضافة بنك جديد تتم فقط عند اختيار المستخدم صراحة “غير ذلك…”

✔ 8.2 التعامل مع المورد

المورد الجديد له شرط واحد واضح:

“يقبل فقط إذا أدخله المستخدم بنفسه أو ظهر في خطاب سابق اعتمده المستخدم.”

غير ذلك → لا يضاف آليًا.

✔ 8.3 تعليمات تنظيف الأسماء Safe Normalization

مسموح:

lowercase

trim

إزالة النقاط

إزالة المسافات المكررة

غير مسموح:

حذف كلمات مثل (bank – company)

إعادة ترتيب الكلمات

تخمين اسم جديد

9) التعليمات الخاصة بالتواريخ

المشروع يقبل:

"2025-10-01"

"01/10/2025"

"1 Oct 2025"

"١ أكتوبر ٢٠٢٥"

Excel Date Serial

المخرج دائمًا:

“01 يناير 2025م”
10) التعليمات الخاصة برفع ملف Excel

يقبل فقط: .xlsx

يرفض أي ترميز غير UTF-8

إذا ظهرت "؟؟؟" → رفض الملف مباشرة

الحد الأدنى للصفوف: ≥ 1

الحد الأقصى للصفوف: 100–150 (مناسب حسب الاستخدام اليومي)

11) الالتزام الصارم بعدم التضخم

يمنع إضافة:

Redux

Zustand

Router

State Machines

Context API معقد

React hooks البسيطة فقط:

useState

useMemo (عند الضرورة فقط)

12) ماذا لو احتجنا تطوير أكبر لاحقًا؟

كل توسّع مستقبلي يجب أن يلتزم:

يعمل داخل نفس الصفحة

لا يضيف تعقيد بصري

لا يطلب من المستخدم خطوات إضافية

لا يعتمد على back-end

13) الخلاصة للمبرمج
✔ اعتمد Vite كسيرفر محلي خفيف
✔ حافظ على واجهة بسيطة – Panels فقط
✔ اجعل كل شيء يعمل في صفحة واحدة
✔ تفاعل مباشر دون أزرار حفظ
✔ تحديث المعاينة يتم تلقائيًا
✔ لا إضافات ولا تعقيد
✔ لا Live Server – لا Python server
✔ كل شيء يجب أن يكون نظيفًا وعمليًا