# .env.example — Example environment variables for guarantee-letters-app
#
# لم يتم العثور على متغيرات بيئة يتم استخدامها حالياً في الكود (لا توجد مراجع لـ `process.env` أو `import.meta.env`).
# هذا الملف يقدّم أمثلة متغيرة وملاحظات لكيفية استخدامها إذا احتجت لإضافة إعدادات بيئية لاحقاً.
#
# ملاحظة: في Vite يجب أن تبدأ المتغيرات التي تحتاج للوصول من جهة العميل بـ `VITE_`.
# انسخ هذا الملف إلى `.env.local` أو `.env` ثم عدّل القيم حسب حاجتك.

# App display / metadata
VITE_APP_TITLE=Guarantee Letters App            # اسم التطبيق الذي يظهر في العنوان
VITE_DEFAULT_LOCALE=ar-SA                       # اللغة/المنطقة الافتراضية
VITE_RELEASE_VERSION=1.0.1                      # نسخة التطبيق (اختياري)

# Backend / API
VITE_API_URL=http://localhost:3000/api          # نقطة نهاية الـ API الخلفية (إن وُجدت)

# Feature flags
VITE_ENABLE_ANALYTICS=false                     # تفعيل/تعطيل التحليلات
VITE_FEATURE_PREVIEW=false                      # تمكين ميزات تجريبية

# Error reporting / monitoring (اختياري)
VITE_SENTRY_DSN=                                # Sentry DSN إذا كنتم تستخدمون Sentry
VITE_SENTRY_ENV=development                     # environment name for monitoring

# Mail / sending (ملاحظة: إرسال البريد من جهة الخادم، عادة ليست متاحة في المتصفح)
VITE_MAILER_ENABLED=false                       # تفعيل إرسال البريد (frontend flag only)
VITE_MAILER_FROM=notifications@example.com      # عنوان المرسل الافتراضي

# Security / secrets — لا تضع مفاتيح حساسة في ملفات التي تُتبع بواسطة git
# احتفظ بالمفاتيح السرية في `.env.local` ولا ترفعها للمستودع.

# End of file
